'use strict';

// #############################################################################

let errors = {
    ERROR_READING_FILE: 'Unable to read file:',
    INFO_ONLY_CSV_SUPPORTED: 'Only .csv files are currently supported, ignored:'
};

module.exports = errors;
